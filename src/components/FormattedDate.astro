---
interface Props {
  date: Date;
  rawDate?: string;
}

const { date, rawDate } = Astro.props;
---

<time datetime={date.toISOString()}>
  {
    rawDate || date.toLocaleDateString("en-us", {
      month: "short", 
      day: "numeric",
      year: "numeric",
    })
  }
</time>
